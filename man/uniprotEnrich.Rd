% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uniprotEnrich.R
\name{uniprotEnrich}
\alias{uniprotEnrich}
\title{uniprotEnrich: Functional Enrichment Analysis for Proteins}
\usage{
uniprotEnrich(
  dt,
  protein.col.name,
  Protein.separator = ";",
  diff.condition.col.name,
  diff.markers = c("Up", "Down", "NotSig"),
  library,
  dt.include.allproteins = TRUE,
  cutoff = 2,
  savefile = "outputfile"
)
}
\arguments{
\item{dt}{A data frame containing protein data. Must include the specified \code{protein.col.name} and \code{diff.condition.col.name} columns.}

\item{protein.col.name}{The column name in \code{dt} that contains protein IDs. Default is "ProteinIDs".}

\item{Protein.separator}{The separator used in the protein ID column. Set to \code{NULL} if no separator is used. Default is ";".}

\item{diff.condition.col.name}{The column name in \code{dt} that contains differential condition markers. Default is "sig".}

\item{diff.markers}{A character vector indicating the differential condition markers (e.g., \code{c("Up", "Down", "NotSig")}). Default is \code{c("Up", "Down", "NotSig")}.}

\item{library}{A data frame containing GO/Pathway annotations for proteins. Columns must include "Entry", "Pathway", and GO-related columns.}

\item{dt.include.allproteins}{Logical, whether \code{dt} includes all proteins (differential and non-differential). Default is \code{TRUE}.}

\item{cutoff}{Numeric, minimum number of proteins in a pathway/GO term to consider for enrichment. Default is \code{2}.}

\item{savefile}{The directory path where results will be saved. Default is "outputfile_test".}
}
\value{
A list containing two elements for each condition (\code{Up}, \code{Down}, etc.):
\describe{
\item{pathway_enrich_result}{A data frame with pathway enrichment results.}
\item{go_enrichment}{A data frame with GO enrichment results (BP, CC, MF).}
}
}
\description{
Perform pathway and GO enrichment analysis using custom protein datasets and libraries.
The function handles differential protein datasets, computes enrichment results, and saves output files.
}
\examples{
# Example usage:
library(ZCPstates)
data("uniprotEnrich.demo.diff", package = "ZCPstates")
data("uniprotEnrich.demo.library.uniprot", package = "ZCPstates")
result <- uniprotEnrich(
  dt = uniprotEnrich.demo.diff,
  library=uniprotEnrich.demo.library.uniprot,
  protein.col.name="Entry",
  Protein.separator=";",
  diff.condition.col.name="Sig",
  diff.markers=c("Up","Down","NotSig"),
  dt.include.allproteins=TRUE,
   cutoff=1,
   savefile="outputfile"
)
}
