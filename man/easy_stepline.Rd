% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/easy_stepline.R
\name{easy_stepline}
\alias{easy_stepline}
\title{Plot Step Line Chart}
\usage{
easy_stepline(
  dt,
  color_map = c("#C0504D", "#4F81BD"),
  x.lab = "sample",
  y.lab = "counts",
  line_width = 0.8
)
}
\arguments{
\item{dt}{A data frame in long format with three columns: \code{Sample}, \code{Group}, and \code{count}.
\itemize{
\item \code{Sample}: Sample names (e.g., "s1", "s2", ...)
\item \code{Group}: Grouping factor (e.g., "A", "B")
\item \code{count}: Numeric values to plot
}}

\item{color_map}{A named character vector specifying colors for each group. Default is c("A" = "#C0504D", "B" = "#4F81BD").}

\item{x.lab}{Character of xlabs.
@param y.lab Character of ylabs.}

\item{line_width}{Numeric value specifying the width of the step lines. Default is 0.8.}
}
\value{
A \code{ggplot} object.
}
\description{
This function draws a step line chart with midpoint steps and value labels.
It supports grouping by method (e.g., "A", "B"), legend customization, and color/line width control.
}
\examples{
dt <- data.frame(
  Sample = rep(paste0("s", 1:7), each = 2),
  Group = rep(c("A", "B"), times = 7),
  count = c(25, 31, 66, 10, 28, 40, 50, 100, 108, 115, 80, 90, 94, 130)
)
easy_stepline(dt)
easy_stepline(dt, color_map = c("darkred", "steelblue"), line_width = 1.2)

}
