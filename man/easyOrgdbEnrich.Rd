% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/easyorgdbEnrich.R
\name{easyOrgdbEnrich}
\alias{easyOrgdbEnrich}
\title{easyOrgdbEnrich: An Easy Wrapper for Functional Enrichment & GSEA Analysis}
\usage{
easyOrgdbEnrich(
  dt,
  proteincol.id = "Protein",
  comparison = "Label",
  savepath = "Report/enrich",
  sep.pattern = ";",
  species = c("org.Hs.eg.db", "org.Mm.eg.db", "org.Rn.eg.db", "org.Dm.eg.db",
    "org.Ce.eg.db", "org.Sc.sgd.db", "org.Dr.eg.db", "org.Bt.eg.db", "org.Gg.eg.db",
    "org.Cf.eg.db", "org.Ss.eg.db")[1],
  background = NULL,
  log2fc.col = "log2FC",
  gsea_widths = 4,
  gsea_heights = 3,
  plotppt = FALSE
)
}
\arguments{
\item{dt}{A data.frame or tibble. Input differential expression results. Must include columns: \code{Protein}, \code{Label}, \code{log2FC}.}

\item{proteincol.id}{Character. Column name in \code{dt} containing UniProt protein IDs. Default is \code{"Protein"}.}

\item{comparison}{Character. Column name in \code{dt} representing comparison groups. Default is \code{"Label"}.}

\item{savepath}{Character. Path to save enrichment results and plots. Default is \code{"Report/enrich"}.}

\item{sep.pattern}{Character. Regex pattern to split multi-ID strings. Default is \code{";"}.}

\item{species}{Character. OrgDb database name (e.g., \code{"org.Hs.eg.db"}). Default is \code{"org.Hs.eg.db"}.}

\item{background}{A background dataframe with columns: \code{Protein}, \code{Label}, and \code{log2FC}, used as universe for enrichment. Default is NULL.}

\item{log2fc.col}{Character. Column name representing log2 fold-change values. Default is \code{"log2FC"}.}

\item{gsea_widths}{Numeric. Width of GSEA plots. Default is \code{4}.}

\item{gsea_heights}{Numeric. Height of GSEA plots. Default is \code{3}.}

\item{plotppt}{Logical. Whether to save figures as PPT using \code{save_zcp()}. Default is \code{FALSE}.}
}
\value{
No value is returned. The function saves enrichment results and GSEA figures to \code{savepath}.
}
\description{
This function performs functional enrichment analysis (KEGG & GO) and GSEA analysis
on proteomics data using UniProt IDs. It supports organism-specific databases (\code{org.*.eg.db})
and provides automatic ID conversion, annotation, visualization (bubble plots and GSEA plots),
and result export.
}
\details{
The function includes:
\itemize{
\item KEGG enrichment analysis using \code{clusterProfiler::enrichKEGG()}
\item GO enrichment analysis using \code{clusterProfiler::enrichGO()}
\item GSEA analysis using \code{clusterProfiler::gseGO()} and \code{gseKEGG()}
\item Protein ID conversion via \code{clusterProfiler::bitr()}
\item Bubble plots using \code{ggplot2}
\item GSEA curve plotting using \code{enrichplot::gseaplot2()}
\item Automatic saving using \code{ZCPstates::save_zcp()}
}
}
\examples{
\dontrun{
easyOrgdbEnrich(
  dt = diff_sig,
  proteincol.id = "Protein",
  comparison = "Label",
  savepath = "Report/enrich",
  sep.pattern = ";",
  species = "org.Hs.eg.db",
  background = NULL,
  log2fc.col = "log2FC"
)
}

}
